var name = "arpa nihan"
if (name.length < 4) {
    name = 'arpa';
}
console.log(name);

//example
var name = "arpa nihan"
if (name.length > 4) {
    name = 'arpa';
}
console.log(name);

//const and let example//
let age = 23; //let will use for changed value//
age = 45;
age = 90;
console.log(age);

const country = 'bangladesh'; //const will use for fixed value//
console.log(country);